services:
  analytics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: service-chatbot-analytics
    environment:
      DATABASE_URL: ${DATABASE_URL}
    ports:
      - "3000:3000"
    restart: unless-stopped
